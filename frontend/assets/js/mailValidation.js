window.addEventListener('load', function() {
    VanillaTilt.init(document.querySelector(".container"), {
        maxTilt: 2,             
        speed: 100,             
        glare: false,           
    });
});
